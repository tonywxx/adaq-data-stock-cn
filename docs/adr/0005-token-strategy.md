# 签名参数:先绕开,后逆为纯 Rust,不内嵌 JS 引擎

东方财富等源的 JS 签名参数(`ut` / `hexin-v`)不通过内嵌 JS 引擎实现——`rquickjs` 依赖已移除,也不引入 `boa` 等其他引擎。第一阶段只做不需要签名的端点;对必须签名的,把其 JS 逆成纯 Rust 小函数。

**Why:** 内嵌 JS 引擎引入重依赖,且需随 akshare 同步 JS;纯 Rust 逆向虽逐个有成本,但无运行时包袱、可长期维护。
**Trade-off:** 全量对齐时需逐个逆向签名端点,工作量集中于此;硬编码/缓存 token 不可持续,故不选。
